# Docker

Docker Compose is defined at the repository root in `docker-compose.yml`.

Service Dockerfiles live next to their applications:

- `apps/api/Dockerfile`
- `apps/worker/Dockerfile`
- `apps/web/Dockerfile`

Keep shared container infrastructure notes and future production deployment
assets in this directory.
